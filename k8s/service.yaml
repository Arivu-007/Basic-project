apiVersion: v1
kind: Service
metadata:
  name: basic-project-service
  namespace: uat
  labels:
    app: basic-project
spec:
  type: LoadBalancer
  selector:
    app: basic-project
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
